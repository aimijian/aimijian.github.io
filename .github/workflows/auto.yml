name: build

on:
  push:
    paths:
      - 'debs/**'
      - 'depictions/**'
      - 'Generate.py'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y dpkg-dev gzip bzip2 python3

      - name: run Generate.py
        run: python3 Generate.py

      - name: commit generated files
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add Packages Packages.gz Packages.bz2 depictions
          git commit -m "auto build [skip ci]" || echo "No changes"
          git push